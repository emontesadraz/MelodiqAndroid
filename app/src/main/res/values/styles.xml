<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema principal de tu aplicación -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/text_color</item> <!-- Color de la ActionBar -->
        <item name="colorPrimaryDark">@color/icon_color</item> <!-- Color de la barra de estado -->
        <item name="android:windowBackground">@color/ic_melodiq_background</item> <!-- Color de fondo de la ventana -->
        <item name="android:statusBarColor">@color/icon_color</item> <!-- Color de la barra de estado -->
        <item name="android:colorBackground">@color/text_color</item> <!-- Color de fondo de la aplicación -->
    </style>
    <style name="BottomNavigationViewStyle" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemIconTint">@null</item>
    </style>
    <style name="Theme.AppCompat.Dialog.MinWidth" parent="Theme.AppCompat.Dialog">
        <item name="windowMinWidthMajor">90%</item>
        <item name="windowMinWidthMinor">90%</item>
    </style>
</resources>